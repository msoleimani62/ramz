# نقشه‌راه پروژه ramz — نسخه ۶ (بعد از دومین راند بازبینی کامل خط‌به‌خط)

> این نسخه بعد از یه بازبینی **واقعاً کامل و بدون فرض** نوشته شده — این‌بار
> روی آخرین نسخه‌ی واقعی پروژه (که خودت `cargo build --workspace
> --all-features` رو روش موفق اجرا کرده بودی)، نه یه snapshot قدیمی. هر
> فایل `.rs` و هر `Cargo.toml` خط‌به‌خط خونده شد، نه فقط چک‌شده بر اساس
> حافظه یا فرض.

---

## چرا راند اول بازبینی کافی نبود

توی راند قبلی، یه اشتباه واقعی افتاد: گفته شد «خط‌به‌خط بازبینی شد» ولی
`mlkem_hybrid.rs` واقعاً دوباره خونده نشد — فرض شد چون قبلاً درست بود، هنوز
درسته. نتیجه‌ش این بود که یه نسخه‌ی خراب از اون فایل (با API ساختگی که
اصلاً توی هیچ نسخه‌ای از crate `ml-kem` وجود نداره) رد شد و باعث خطای build
شد. این درسیه که این‌بار جدی گرفته شد: **هیچ فایلی رو بدون خوندن کامل
محتواش، «درست» فرض نکردیم** — حتی فایل‌هایی که قبلاً یه‌بار تأیید شده بودن.

---

## یافته‌های این راند (۱۰ مورد، همه رفع شدن)

1. **`mlkem_hybrid.rs` خراب بود** — به یه API غیرواقعی (`KemCore`,
   `Encoded`, `EncodedSizeUser`, `MlKem768Params`, `.generate()`) برگشته
   بود که هیچ‌جای crate واقعی وجود نداره. با نسخه‌ی تأییدشده (که مستقیم از
   سورس واقعی crate چک شده بود) جایگزین شد.
2. **پرامپت تکراری/گیج‌کننده توی `ramz verify --identity`** — همیشه اول یه
   «Enter password:» عمومی می‌پرسید، حتی برای آرشیو identity-based، و بعد
   یه پرامپت دوم مخصوص identity. الان فقط وقتی `--identity` داده بشه، فقط
   پرامپت identity پرسیده می‌شه.
3. **`ramz extract` بدون پسورد/identity اصلاً پرامپت نمی‌داد** — برخلاف
   `pack` و `verify`، مستقیم با خطا fail می‌شد. الان مثل بقیه‌ی دستورات
   تعاملی پرامپت می‌ده.
4. **`Cargo.lock` توی `.gitignore` بود** — برای یه پروژه‌ی باینری (نه
   کتابخونه)، این برخلاف رسمه؛ باید commit بشه برای build قابل‌بازتولید.
5. **یه فایل `LICENSE` قدیمی و اضافی** از یه merge قبلی جا مونده بود، کنار
   `LICENSE-MIT`/`LICENSE-APACHE` جدید — حذف شد.
6. **`docs/IDENTITY_FORMAT.md` هنوز فرمت قدیمی رو توصیف می‌کرد** (کلید
   ذخیره‌سازی نشتی که قبلاً حذفش کرده بودیم) — با فرمت واقعی فعلی (بایت
   فلگ محافظت) هماهنگ شد.
7. **پوشش تستی بک‌اند 7z توی `integration-tests` تقریباً صفر بود** — فقط یه
   تست متادیتا مونده بود، هیچ pack/verify/extract واقعی تست نمی‌شد؛ دو تست
   کامل (roundtrip + رد پسورد غلط) اضافه شد.
8. `resume_state_path()` فرمتش از `.ramz-resume` به `.resume` (از طریق
   `Path::with_extension`) تغییر کرده بود — این خودش باگ نبود، ولی تأیید
   شد که `.gitignore` با الگوی جدید (`*.resume`) هماهنگه.
9. شمارش تست‌ها و ادعای README/CHANGELOG به‌روز شد (الان ۸۴ تست، بعد از
   اضافه‌شدن تست‌های 7z).
10. یادداشت‌های CHANGELOG برای همه‌ی موارد بالا اضافه شد تا تاریخچه دقیق
    بمونه.

---

## لیست ب — فازهای بلندمدت (بدون تغییر، هنوز باز)

### ب-۱. پشتیبانی از چند گیرنده هم‌زمان (multi-recipient)
مثل `age -r key1 -r key2`. نیاز به تصمیم طراحی: secret مشترک برای همه یا
مستقل برای هرکدوم. نسخه‌بندی پیشنهادی: v0.4.0.

### ب-۲. بررسی امنیتی مستقل (Security Audit)
قبل از هر ادعای production-ready، طراحی `RMZ1` و `RIM1` باید توسط یه نفر
متخصص رمزنگاری مستقل مرور بشه — این با اضافه‌شدن ادعای واقعی post-quantum
اهمیت بیشتری پیدا کرده.

### ب-۳. رمزنگاری Streaming/Chunked برای فایل‌های خیلی بزرگ
Pipeline فعلی همه‌چیز رو توی RAM/temp file نگه می‌داره، نه streaming واقعی.

### ب-۴. توزیع پکیج (crates.io، AUR)
بعد از تأیید نهایی CI/باینری‌های پیش‌ساخته.

---

## نمره‌ی نهایی

**۹ از ۱۰** (بدون تغییر نسبت به راند قبل، چون کیفیت معماری و طراحی امنیتی
همچنان بالاست — این راند فقط باگ‌های اجرایی/رگرسیونی رو گرفت، نه مشکل
طراحی جدید).

چرا نه ۱۰: تعداد و تنوع باگ‌هایی که فقط با یه بازبینی مستقل و کامل (نه
فرض بر پایه‌ی حافظه) پیدا شدن، نشون می‌ده فرآیند merge/rebase بین
session‌های مختلف کار روی این پروژه هنوز خیلی شکننده‌ست — چند بار همون
باگ (resume، mlkem API) دوباره برگشته. این یه مشکل *فرآیندی*ه (نه معماری)،
ولی برای یه ابزار امنیتی مهمه که قبل از هر release، حتماً یه چک نهایی کامل
(`cargo test --workspace --all-features` + بازبینی دستی diff) انجام بشه.

---

## این پروژه الان در چه سطحیه؟ (ارزیابی صادقانه)

**سطح فعلی: یه ابزار CLI شخصی/تیمی با کیفیت بالا، در آستانه‌ی release
عمومی محدود (نه enterprise/production در مقیاس بزرگ).**

### چرا این سطح؟
- معماری تمیز، مدولار، با تست‌های واقعی (۸۴ تست، نه فقط smoke test).
- تصمیمات امنیتی **صادقانه مستند شدن** (مثلاً این‌که `--mlkem` واقعاً
  چی رو محافظت می‌کنه و چی رو نه) — این نادره حتی توی پروژه‌های
  متن‌باز بزرگ‌تر.
- CI/CD واقعی، چندپلتفرمی (۷ هدف build).
- ولی: هیچ‌وقت توسط یه cryptographer مستقل بازبینی نشده، فرمت باینری
  (`RMZ1`/`RIM1`) خودساخته‌ست (نه یه استاندارد جاافتاده مثل `age` خالص)،
  و streaming واقعی نداره.

### کاربردهای واقعی که الان می‌تونه داشته باشه:
1. **بکاپ شخصی رمزشده** — فایل‌های حساس شخصی (اسناد، عکس، پشتیبان
   پسوردها) روی فضای ابری غیرقابل‌اعتماد (Google Drive، Dropbox) یا
   USB. دقیقاً همون use case اولیه‌ی پروژه.
2. **اشتراک‌گذاری امن فایل بین دو نفر که به هم اعتماد دارن** — با
   `--recipient`، می‌شه برای یه دوست/همکار مشخص، بدون رد و بدل کردن
   پسورد از یه کانال جدا، فایل امن فرستاد (شبیه `age -r`).
3. **آرشیو مقاوم در برابر آینده (long-term archival)** — کسی که می‌خواد
   امروز چیزی رو رمز کنه و مطمئن باشه ۱۰-۲۰ سال دیگه هم (حتی با ظهور
   کامپیوتر کوانتومی) باز نشه، با `--recipient` این تضمین واقعی رو داره.
4. **جایگزین ابزارهای خط‌فرمان مشابه (`age`, `gpg`, `7z`) برای کسی که
   می‌خواد یه ابزار واحد با گزینه‌های بیشتر (Argon2id، secure-delete،
   resume) بخواد**، بدون این‌که مجبور باشه چندتا ابزار جدا رو یاد بگیره.

### کاربردهایی که **هنوز** براش آماده نیست:
- محیط‌های enterprise/regulatory (HIPAA، PCI-DSS، و مشابه) که نیاز به
  تأیید امنیتی رسمی (FIPS validation، audit مستقل) دارن.
- ذخیره‌سازی فایل‌های خیلی بزرگ (چند ده گیگابایت) به‌خاطر نبود streaming.
- سناریوهای multi-party (بیش از یه گیرنده) تا وقتی ب-۱ پیاده بشه.
- هر جایی که «تضمین ۱۰۰٪» لازمه، نه «سخت‌تر کردن معقول» (مثل حذف امن
  روی SSD که صادقانه گفتیم تضمین فیزیکی نیست).

**جمع‌بندی یک‌خطی:** این ابزار برای استفاده‌ی شخصی/تیمی جدی، همین الان
آماده‌ست؛ برای ادعای «production-grade security tool» عمومی، فقط بررسی
امنیتی مستقل (ب-۲) مونده.
