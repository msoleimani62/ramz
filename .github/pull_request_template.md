## English

**What does this PR do?**
Short description of the change.

**Related issue**
Closes #

**Checklist**
- [ ] `cargo test --workspace --all-features` passes
- [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings` is clean
- [ ] `cargo fmt --all` was run
- [ ] Added/updated tests for the change
- [ ] Updated `CHANGELOG.md` if user-facing
- [ ] Updated `README.md` / `docs/` if behavior changed

---

## فارسی

**این PR چیکار می‌کنه؟**
توضیح کوتاه تغییر.

**issue مرتبط**
Closes #

**چک‌لیست**
- [ ] `cargo test --workspace --all-features` پاس می‌شه
- [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings` تمیزه
- [ ] `cargo fmt --all` اجرا شده
- [ ] تست برای این تغییر اضافه/به‌روز شده
- [ ] `CHANGELOG.md` (اگه کاربر نهایی رو تحت تأثیر قرار می‌ده) به‌روز شده
- [ ] `README.md`/`docs/` (اگه رفتار عوض شده) به‌روز شده
